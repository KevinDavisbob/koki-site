---
title: "Markdown Writing Guide"
date: "2026-08-10"
description: "A sample post demonstrating every formatting style this site supports: headings, lists, tables, code blocks, quotes, and more."
tags: ["Tutorial", "Markdown"]
---

This post demonstrates the Markdown styles supported on this site. Use it as a reference when writing new posts.

## Heading level 2

### Heading level 3

Body text: **bold**, *italic*, ~~strikethrough~~, `inline code`, and [links](https://nextjs.org).

## Lists

Ordered list:

1. First item
2. Second item
3. Third item

Unordered list:

- Apple
- Banana
- Orange

Task list:

- [x] Set up the site
- [ ] Write the first post
- [ ] Ship it

## Code blocks

Syntax highlighting with light and dark themes:

```python
def fib(n: int) -> int:
    """Fibonacci sequence"""
    a, b = 0, 1
    for _ in range(n):
        a, b = b, a + b
    return a
```

```bash
# Command line works too
npm run dev
```

## Tables

| Language | Use case | Difficulty |
| -------- | -------- | ---------- |
| TypeScript | Frontend / full-stack | ★★★ |
| Python | Scripting / data | ★★ |
| Rust | Systems / performance | ★★★★★ |

## Quotes and dividers

> Good writing is rewriting.

---

That covers every style this site supports — feel free to copy this document as a template for new posts.
